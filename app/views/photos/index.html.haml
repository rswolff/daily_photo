%h1 Past Photos

- if can? :create, Photo
  %p= link_to 'New photo', new_photo_path, class: 'btn primary'

%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Published
      %th= Photo.human_attribute_name :title
      -if can? :edit, Photo
        %th Actions
  
  %tbody
    - for photo in @photos
      %tr{ :class => cycle(:odd, :even) }
        %td{class: 'span3'}= photo.created_at.to_s(:long)
        %td{class: 'span9'}= link_to photo.title, photo
        -if can? :edit, Photo
          %td= link_to 'Edit Photo', edit_photo_path(photo), class: 'btn'
